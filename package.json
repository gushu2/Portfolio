{
  "name": "gushu-bhat-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3",
    "uuid": "^14.0.0"
  }
}
