{
  "name": "gushu-bhat-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3",
    "uuid": "^14.0.0"
  }
}
